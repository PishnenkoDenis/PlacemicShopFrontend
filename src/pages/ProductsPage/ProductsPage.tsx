import React, { memo } from 'react';

import styles from './productsPage.module.scss';

const ProductsPage = () => {
  return <div className={styles.pageWrapper}>ProductsPage</div>;
};

export default memo(ProductsPage);
