import React, { memo } from 'react';

import styles from './settingsPage.module.scss';

const SettingsPage = () => {
  return <div className={styles.pageWrapper}>SettingsPage</div>;
};

export default memo(SettingsPage);
